name: Reusable CI

on:
  workflow_call:

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Placeholder check
        run: |
          echo "Reusable CI workflow from thebigtechplus/.github"
          echo "Extend this workflow or override jobs in consuming repositories as products are added."
